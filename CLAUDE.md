# Claude Codeへの指示

このフォルダは「クチズムあいうえお — ひらがな復習アプリ」のMVP実装用です。

## 最初に必ず行うこと

1. `MVP_SPEC.md` を最後まで読む。
2. `npm run validate` を実行する。
3. 検査が失敗した場合は、素材を代用・改変せず、不足や不一致を報告して止める。

## 実装方針

- HTML・CSS・JavaScriptで、静的なWebアプリとして実装する。
- GitHub Pagesで公開できる構成にする。
- 外部フレームワークやビルド工程は追加しない。
- 確定素材を加工・上書きしない。
- 文字・単語・読み上げフレーズ・画像パスは `js/hiragana-data.js` を唯一の正とする。
- `difficult` は参考情報として保持するが、MVPの抽選には使用しない。
- 画面の動的要素はHTML/CSSで実装し、背景画像へ焼き込まない。
- 子ども向け画面の文言には漢字を使わない。
- 音声・画面遷移・二重入力防止の詳細は `MVP_SPEC.md` に従う。

## 作成する主なファイル

- `index.html`
- `css/style.css`
- `js/app.js`

必要最小限の補助ファイルは追加してよい。ただし、確定仕様を変える判断はしないこと。

## 完了時の確認

1. `npm run validate` が成功する。
2. `MVP_SPEC.md` の「MVPの完成条件」を1項目ずつ確認する。
3. スマートフォンとタブレットの横画面で確認する。
4. ブラウザのコンソールエラーがないことを確認する。
5. 実装した内容、確認結果、残課題を簡潔に報告する。

