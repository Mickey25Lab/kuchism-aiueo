# クチズムあいうえお — ひらがな復習アプリ MVP仕様書

更新日: 2026-09-16  
ステータス: 初稿（読み上げ方式Fix済み・Kimiレビュー中）

## 1. プロダクトの目的

一度覚えたひらがなを忘れかけている幼児が、短時間で楽しく復習できるWebアプリを作る。

新しくひらがなを教える学習アプリではなく、覚えているかを確認する「復習アプリ」とする。

## 2. MVPの対象

- 主な利用者: 3歳前後の幼児
- 最初の実利用者: Sumire
- 利用端末: スマートフォンおよびタブレット
- 画面方向: 横向きを優先
- 出題対象: 基本のひらがな46文字
- 1回の問題数: 20問
- 回答方法: 子どもが表示されたひらがなを読む
- 正誤判定: 読み方を聞いた保護者が3つの判定ボタンから選ぶ
- 音声認識: 使用しない
- ログイン・アカウント: 作らない
- サーバー・データベース: 使用しない

## 3. 1回の復習の流れ

1. スタート画面を表示する。
2. 「はじめる」を押す。
3. 46文字から20文字をランダムに選ぶ。
4. 同じ回の中では同じ文字を重複させない。
5. 1文字ずつ出題する。
6. 子どもが表示されたひらがなを読む。
7. 保護者が「できた」「どうかな」「できなかった」のいずれかを押す。
8. 選択に応じた効果音と復習画像・読み上げを再生する。
9. 20問終了後、結果画面を表示する。
10. 「もういっかい」を押すと、新しい20文字を再抽選して最初から始める。

## 4. 画面構成

MVPでは実質3種類の画面を使用する。「できた」後・「どうかな」後・「できなかった」後は、新規ページへ移動せず、出題画面内の表示を切り替える。

### 4.1 スタート画面

- デザイン見本: `screen_01_start.png`
- 実装用背景画像: `screen_01_start_bg.webp`
- 「はじめる」ボタンは背景画像に含めず、HTML/CSSで重ねる。
- 表示文言: 「クチズムあいうえお」「ひらがな おぼえたかな？」
- 操作要素: 「はじめる」ボタン
- 「はじめる」で20問を抽選し、1問目へ進む。

### 4.2 出題・復習画面

- デザイン見本: 進捗・出題文字・3つの判定ボタンを含む出題画面
- 実装用背景画像: `screen_02_question_bg.webp`
- 進捗、出題文字、3つの判定ボタンは背景画像に含めず、HTML/CSSで重ねる。

#### 出題中

- 上部に進捗を表示する。例: 「1もんめ / 20」
- 中央に出題するひらがな1文字を大きく表示する。
- 下部に次の3ボタンを横並びで表示する。
  - 「できた」＋にっこり顔
  - 「どうかな」＋普通の顔
  - 「できなかった」＋残念そうだが強く落ち込まない顔
- 1問につき、いずれか1回だけ回答できる。
- 3つのボタンは保護者が判定操作に使用する。ただし、子どもにも結果が直感的に伝わるデザインにする。
- 回答後はボタンを無効化し、二重入力を防ぐ。

#### 「できた」を押した後

1. 効果音 `sfx_correct_pikoon.mp3` を再生する。
2. 出題文字に対応する完成済み復習画像を表示する。
3. 対応フレーズを読み上げる。例: 「あひるのあ」
4. 読み上げ終了後1秒間待つ。
5. 自動的に次の問題へ進む。

#### 「どうかな」を押した後

1. 効果音 `sfx_unsure_bururun.mp3` を再生する。
2. 出題文字に対応する完成済み復習画像を表示する。
3. 対応フレーズを読み上げる。例: 「あひるのあ」
4. 読み上げ終了後1秒間待つ。
5. 自動的に次の問題へ進む。

#### 「できなかった」を押した後

1. 効果音 `sfx_incorrect_bu.mp3` を再生する。
2. 出題文字に対応する完成済み復習画像を表示する。
3. 対応フレーズを読み上げる。例: 「あひるのあ」
4. 「どうぞ」と読み上げる。
5. 「どうぞ」の読み上げ終了時点から3秒間待つ。
6. 自動的に次の問題へ進む。

### 4.3 結果画面

- デザイン見本: `screen_05_result.png`
- 実装用背景画像: `screen_05_result_bg.webp`
- 3つの結果数字と「もういっかい」ボタンは背景画像に含めず、HTML/CSSで重ねる。
- 上部: 「20もん おわったよ！」
- 3分類の件数を大きく表示する。
  - 緑・にっこり顔: 「できた」
  - 黄・普通の顔: 「どうかな」
  - 薄いコーラル色・やさしい残念顔: 「できなかった」
- 3つの件数の合計は必ず20になる。
- 励まし文言: 「さいごまで できたね！」
- 操作要素: 「もういっかい」ボタン
- 子ども向け文言には漢字を使わない。
- 数字は実際の結果に応じて動的に差し替える。Final画像内の `12 / 5 / 3` はレイアウト例であり固定値ではない。

## 5. データ構造

ひらがなごとに、最低限次の情報を1つのデータとして管理する。

```js
{
  id: 1,
  character: "あ",
  word: "あひる",
  phrase: "あひるのあ",
  image: "assets/images/hiragana/01_a_ahiru.webp",
  difficult: true
}
```

- 46文字分を1つの配列またはJSONファイルにまとめる。
- 画面側に文字・単語・ファイルパスを個別に直書きしない。
- 画像ファイルとの対応付けは、このデータを唯一の正として管理する。
- ファイル名に連番を付け、並びと不足を機械的に確認できるようにする。
- `difficult` は元の確認結果を残す参考情報とし、MVPのランダム出題では重み付けに使用しない。

## 6. 回答データ

各問題について次を保持する。

```js
{
  character: "あ",
  result: "correct" // correct | unsure | incorrect
}
```

セッション中に必要な状態:

- 抽選された20文字
- 現在の問題番号
- 各文字の回答結果
- 「できた」の件数
- 「どうかな」の件数
- 「できなかった」の件数

ページを再読み込みした場合の復元は、初回MVPの対象外とする。

## 7. 素材フォルダ案

```text
hiragana-review-app/
├─ index.html
├─ css/
│  └─ style.css
├─ js/
│  ├─ app.js
│  └─ hiragana-data.js
├─ assets/
│  ├─ screens/
│  │  ├─ screen_01_start_bg.webp
│  │  ├─ screen_02_question_bg.webp
│  │  └─ screen_05_result_bg.webp
│  ├─ images/
│  │  └─ hiragana/
│  │     └─ （46文字の完成画像）
│  └─ audio/
│     ├─ effects/
│     │  ├─ sfx_correct_pikoon.mp3
│     │  ├─ sfx_unsure_bururun.mp3
│     │  └─ sfx_incorrect_bu.mp3
└─ README.md
```

## 8. 読み上げ方式

- MVPではブラウザ標準の音声読み上げ機能（Web Speech API / `speechSynthesis`）を使用する。
- 読み上げ言語は日本語（`ja-JP`）を指定する。
- 読み上げる文章は、各文字の `phrase` を使用する。例: 「あひるのあ」
- 「できなかった」の場合は、フレーズの後に「どうぞ」と読み上げる。
- 「どうぞ」の読み上げが終わってから3秒後に、次の問題へ進む。
- 同時に複数の読み上げを実行しない。新しい読み上げ前に残っている発話をキャンセルする。
- 日本語音声が複数ある場合は、利用可能な日本語音声を1つ選択する。特定の端末固有音声名には依存しない。
- 読み上げ処理は1つの関数にまとめ、将来、固定音声ファイルへ交換できる構造にする。
- 読み上げが利用できないブラウザでは、画面表示は継続し、コンソールに警告を出す。

## 9. UI・実装ルール

- 幼児がタップしやすいよう、ボタンは十分に大きくする。
- 子ども向け文言に漢字を使わない。
- 強い赤色、罰、バツ印など、失敗を強く感じさせる表現を使わない。
- 効果音と読み上げ音声を重ねて再生しない。
- 音声再生中の連打・二重遷移を防ぐ。
- 画像の縦横比を崩さない。
- 画面背景には軽量化したWebPを使用する。デザイン見本のPNGを本番表示に使用しない。
- 背景画像内に操作ボタンや動的な数字を埋め込まない。ボタン・数字はHTML/CSSで実装する。
- 主要部分はスクロールなしで1画面に収める。
- スマートフォンとタブレットの横画面でレイアウトが破綻しないようにする。
- 外部フレームワークは必須とせず、HTML・CSS・JavaScriptで実装できる構成を優先する。

## 10. MVPの完成条件

- スタート画面から20問を開始できる。
- 46文字から重複なしで20文字がランダムに選ばれる。
- 20問すべてで3つの回答ボタンが動作する。
- 回答に合った効果音が鳴る。
- 対応する復習画像が表示される。
- 対応フレーズが読み上げられる。
- 「できなかった」の場合のみ「どうぞ」が入る。
- 20問終了後、3分類の件数が正しく表示される。
- 3分類の合計が20になる。
- 「もういっかい」で新しい20問を開始できる。
- ブラウザのコンソールにエラーが出ない。

## 11. MVP対象外

- 音声認識による自動判定
- ユーザー登録・ログイン
- 複数ユーザー管理
- 学習履歴の長期保存
- 忘却曲線に基づく出題最適化
- 保護者用管理画面
- 苦手文字の自動優先出題
- サーバー・データベース
- 課金機能

## 12. 画面遷移タイミング（Fix済み）

- 「できた」: フレーズ読み上げ終了から1秒後に次の問題へ進む。
- 「どうかな」: フレーズ読み上げ終了から1秒後に次の問題へ進む。
- 「できなかった」: フレーズの後に「どうぞ」と読み上げ、その終了から3秒後に次の問題へ進む。

## 13. Claude Codeへの実装指示

この仕様書と支給素材を読み、まず静的なWebアプリとして実装すること。

- 実装前に素材一覧を取得し、46文字すべての画像が存在するか検査する。
- `hiragana-data.js` の文字・単語・フレーズ・画像パスを機械的に照合する。
- 素材が不足している場合、別の素材で代用せず、不足一覧を報告する。
- 既存の素材ファイルを勝手に加工・上書きしない。
- まずローカルで動作確認し、その後GitHub Pagesで公開できる構成にする。
- 実装後、完成条件を1項目ずつ確認して結果を報告する。
